<div class="section">
    <div class="container">
        <div id="promo">
            <div class="jumbotron">
                <h3><%= service.name %></h3>
                <p class="text-justify"><%- service.description %></p>
                <p></p>
            </div>
        </div>
        <% service.products.forEach(function(product, i) { %>
        <% if(i % 2 == 0) { %>
        <div class="row">
            <% } %>
            <div class="col-md-6">
                <% include product.ejs %>
            </div>
            <% if(i % 2 == 1 || i == service.products.length-1 ) { %>
        </div>
        <% } %>

        <% }); %>
    </div>
</div>